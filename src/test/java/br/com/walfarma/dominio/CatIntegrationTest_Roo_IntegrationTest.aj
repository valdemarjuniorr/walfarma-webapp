// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.com.walfarma.dominio;

import br.com.walfarma.dominio.CatDataOnDemand;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect CatIntegrationTest_Roo_IntegrationTest {
    
    declare @type: CatIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: CatIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext.xml");
    
    declare @type: CatIntegrationTest: @Transactional;
    
    @Autowired
    private CatDataOnDemand CatIntegrationTest.dod;
    
    @Test
    public void CatIntegrationTest.testCountCats() {
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", dod.getRandomCat());
        long count = br.com.walfarma.dominio.Cat.countCats();
        org.junit.Assert.assertTrue("Counter for 'Cat' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void CatIntegrationTest.testFindCat() {
        br.com.walfarma.dominio.Cat obj = dod.getRandomCat();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Cat.findCat(id);
        org.junit.Assert.assertNotNull("Find method for 'Cat' illegally returned null for id '" + id + "'", obj);
        org.junit.Assert.assertEquals("Find method for 'Cat' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void CatIntegrationTest.testFindAllCats() {
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", dod.getRandomCat());
        long count = br.com.walfarma.dominio.Cat.countCats();
        org.junit.Assert.assertTrue("Too expensive to perform a find all test for 'Cat', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        java.util.List<br.com.walfarma.dominio.Cat> result = br.com.walfarma.dominio.Cat.findAllCats();
        org.junit.Assert.assertNotNull("Find all method for 'Cat' illegally returned null", result);
        org.junit.Assert.assertTrue("Find all method for 'Cat' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void CatIntegrationTest.testFindCatEntries() {
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", dod.getRandomCat());
        long count = br.com.walfarma.dominio.Cat.countCats();
        if (count > 20) count = 20;
        java.util.List<br.com.walfarma.dominio.Cat> result = br.com.walfarma.dominio.Cat.findCatEntries(0, (int) count);
        org.junit.Assert.assertNotNull("Find entries method for 'Cat' illegally returned null", result);
        org.junit.Assert.assertEquals("Find entries method for 'Cat' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void CatIntegrationTest.testFlush() {
        br.com.walfarma.dominio.Cat obj = dod.getRandomCat();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Cat.findCat(id);
        org.junit.Assert.assertNotNull("Find method for 'Cat' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyCat(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        obj.flush();
        org.junit.Assert.assertTrue("Version for 'Cat' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CatIntegrationTest.testMerge() {
        br.com.walfarma.dominio.Cat obj = dod.getRandomCat();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Cat.findCat(id);
        boolean modified =  dod.modifyCat(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        br.com.walfarma.dominio.Cat merged = (br.com.walfarma.dominio.Cat) obj.merge();
        obj.flush();
        org.junit.Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        org.junit.Assert.assertTrue("Version for 'Cat' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void CatIntegrationTest.testPersist() {
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", dod.getRandomCat());
        br.com.walfarma.dominio.Cat obj = dod.getNewTransientCat(Integer.MAX_VALUE);
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to provide a new transient entity", obj);
        org.junit.Assert.assertNull("Expected 'Cat' identifier to be null", obj.getId());
        obj.persist();
        obj.flush();
        org.junit.Assert.assertNotNull("Expected 'Cat' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void CatIntegrationTest.testRemove() {
        br.com.walfarma.dominio.Cat obj = dod.getRandomCat();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to initialize correctly", obj);
        java.lang.Long id = obj.getId();
        org.junit.Assert.assertNotNull("Data on demand for 'Cat' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Cat.findCat(id);
        obj.remove();
        obj.flush();
        org.junit.Assert.assertNull("Failed to remove 'Cat' with identifier '" + id + "'", br.com.walfarma.dominio.Cat.findCat(id));
    }
    
}
