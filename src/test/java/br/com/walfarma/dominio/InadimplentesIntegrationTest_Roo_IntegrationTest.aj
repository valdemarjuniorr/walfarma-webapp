// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.com.walfarma.dominio;

import br.com.walfarma.dominio.InadimplentesDataOnDemand;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect InadimplentesIntegrationTest_Roo_IntegrationTest {
    
    declare @type: InadimplentesIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: InadimplentesIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext.xml");
    
    declare @type: InadimplentesIntegrationTest: @Transactional;
    
    @Autowired
    private InadimplentesDataOnDemand InadimplentesIntegrationTest.dod;
    
    @Test
    public void InadimplentesIntegrationTest.testCountInadimplenteses() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", dod.getRandomInadimplentes());
        long count = br.com.walfarma.dominio.Inadimplentes.countInadimplenteses();
        org.junit.Assert.assertTrue("Counter for 'Inadimplentes' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void InadimplentesIntegrationTest.testFindInadimplentes() {
        br.com.walfarma.dominio.Inadimplentes obj = dod.getRandomInadimplentes();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", obj);
        java.lang.Long id = obj.get_id();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Inadimplentes.findInadimplentes(id);
        org.junit.Assert.assertNotNull("Find method for 'Inadimplentes' illegally returned null for id '" + id + "'", obj);
        org.junit.Assert.assertEquals("Find method for 'Inadimplentes' returned the incorrect identifier", id, obj.get_id());
    }
    
    @Test
    public void InadimplentesIntegrationTest.testFindAllInadimplenteses() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", dod.getRandomInadimplentes());
        long count = br.com.walfarma.dominio.Inadimplentes.countInadimplenteses();
        org.junit.Assert.assertTrue("Too expensive to perform a find all test for 'Inadimplentes', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        java.util.List<br.com.walfarma.dominio.Inadimplentes> result = br.com.walfarma.dominio.Inadimplentes.findAllInadimplenteses();
        org.junit.Assert.assertNotNull("Find all method for 'Inadimplentes' illegally returned null", result);
        org.junit.Assert.assertTrue("Find all method for 'Inadimplentes' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void InadimplentesIntegrationTest.testFindInadimplentesEntries() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", dod.getRandomInadimplentes());
        long count = br.com.walfarma.dominio.Inadimplentes.countInadimplenteses();
        if (count > 20) count = 20;
        java.util.List<br.com.walfarma.dominio.Inadimplentes> result = br.com.walfarma.dominio.Inadimplentes.findInadimplentesEntries(0, (int) count);
        org.junit.Assert.assertNotNull("Find entries method for 'Inadimplentes' illegally returned null", result);
        org.junit.Assert.assertEquals("Find entries method for 'Inadimplentes' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void InadimplentesIntegrationTest.testFlush() {
        br.com.walfarma.dominio.Inadimplentes obj = dod.getRandomInadimplentes();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", obj);
        java.lang.Long id = obj.get_id();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Inadimplentes.findInadimplentes(id);
        org.junit.Assert.assertNotNull("Find method for 'Inadimplentes' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyInadimplentes(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        obj.flush();
        org.junit.Assert.assertTrue("Version for 'Inadimplentes' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void InadimplentesIntegrationTest.testMerge() {
        br.com.walfarma.dominio.Inadimplentes obj = dod.getRandomInadimplentes();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", obj);
        java.lang.Long id = obj.get_id();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Inadimplentes.findInadimplentes(id);
        boolean modified =  dod.modifyInadimplentes(obj);
        java.lang.Integer currentVersion = obj.getVersion();
        br.com.walfarma.dominio.Inadimplentes merged = (br.com.walfarma.dominio.Inadimplentes) obj.merge();
        obj.flush();
        org.junit.Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.get_id(), id);
        org.junit.Assert.assertTrue("Version for 'Inadimplentes' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void InadimplentesIntegrationTest.testPersist() {
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", dod.getRandomInadimplentes());
        br.com.walfarma.dominio.Inadimplentes obj = dod.getNewTransientInadimplentes(Integer.MAX_VALUE);
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to provide a new transient entity", obj);
        org.junit.Assert.assertNull("Expected 'Inadimplentes' identifier to be null", obj.get_id());
        obj.persist();
        obj.flush();
        org.junit.Assert.assertNotNull("Expected 'Inadimplentes' identifier to no longer be null", obj.get_id());
    }
    
    @Test
    public void InadimplentesIntegrationTest.testRemove() {
        br.com.walfarma.dominio.Inadimplentes obj = dod.getRandomInadimplentes();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to initialize correctly", obj);
        java.lang.Long id = obj.get_id();
        org.junit.Assert.assertNotNull("Data on demand for 'Inadimplentes' failed to provide an identifier", id);
        obj = br.com.walfarma.dominio.Inadimplentes.findInadimplentes(id);
        obj.remove();
        obj.flush();
        org.junit.Assert.assertNull("Failed to remove 'Inadimplentes' with identifier '" + id + "'", br.com.walfarma.dominio.Inadimplentes.findInadimplentes(id));
    }
    
}
