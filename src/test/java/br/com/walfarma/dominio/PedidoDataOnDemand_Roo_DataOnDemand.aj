// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.com.walfarma.dominio;

import br.com.walfarma.dominio.Pedido;
import java.util.List;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect PedidoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: PedidoDataOnDemand: @Component;
    
    private Random PedidoDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Pedido> PedidoDataOnDemand.data;
    
    public Pedido PedidoDataOnDemand.getNewTransientPedido(int index) {
        br.com.walfarma.dominio.Pedido obj = new br.com.walfarma.dominio.Pedido();
        obj.setId(new Integer(index).longValue());
        obj.setProduto(null);
        obj.setSaldo(java.math.BigDecimal.valueOf(index));
        obj.setTotalVenda(java.math.BigDecimal.valueOf(index));
        obj.setMediaVenda(java.math.BigDecimal.valueOf(index));
        obj.setQuantidadePedido(new Integer(index));
        obj.setTipoInclusao(new Integer(index).shortValue());
        return obj;
    }
    
    public Pedido PedidoDataOnDemand.getSpecificPedido(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Pedido obj = data.get(index);
        return Pedido.findPedido(obj.get_id());
    }
    
    public Pedido PedidoDataOnDemand.getRandomPedido() {
        init();
        Pedido obj = data.get(rnd.nextInt(data.size()));
        return Pedido.findPedido(obj.get_id());
    }
    
    public boolean PedidoDataOnDemand.modifyPedido(Pedido obj) {
        return false;
    }
    
    public void PedidoDataOnDemand.init() {
        data = br.com.walfarma.dominio.Pedido.findPedidoEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Pedido' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<br.com.walfarma.dominio.Pedido>();
        for (int i = 0; i < 10; i++) {
            br.com.walfarma.dominio.Pedido obj = getNewTransientPedido(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
