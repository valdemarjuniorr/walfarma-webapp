// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.com.walfarma.dominio;

import br.com.walfarma.dominio.Banco;
import java.util.List;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect BancoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: BancoDataOnDemand: @Component;
    
    private Random BancoDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Banco> BancoDataOnDemand.data;
    
    public Banco BancoDataOnDemand.getNewTransientBanco(int index) {
        br.com.walfarma.dominio.Banco obj = new br.com.walfarma.dominio.Banco();
        obj.setId(new Integer(index).longValue());
        java.lang.String nome = "nome_" + index;
        if (nome.length() > 20) {
            nome  = nome.substring(0, 20);
        }
        obj.setNome(nome);
        java.lang.String codigoBanco = "c_" + index;
        if (codigoBanco.length() > 3) {
            codigoBanco  = codigoBanco.substring(0, 3);
        }
        obj.setCodigoBanco(codigoBanco);
        obj.setDigitoBA("digitoBA_" + index);
        return obj;
    }
    
    public Banco BancoDataOnDemand.getSpecificBanco(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Banco obj = data.get(index);
        return Banco.findBanco(obj.get_id());
    }
    
    public Banco BancoDataOnDemand.getRandomBanco() {
        init();
        Banco obj = data.get(rnd.nextInt(data.size()));
        return Banco.findBanco(obj.get_id());
    }
    
    public boolean BancoDataOnDemand.modifyBanco(Banco obj) {
        return false;
    }
    
    public void BancoDataOnDemand.init() {
        data = br.com.walfarma.dominio.Banco.findBancoEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Banco' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<br.com.walfarma.dominio.Banco>();
        for (int i = 0; i < 10; i++) {
            br.com.walfarma.dominio.Banco obj = getNewTransientBanco(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
