// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.com.walfarma.dominio;

import br.com.walfarma.dominio.Tributacao;
import java.util.List;
import java.util.Random;
import org.springframework.stereotype.Component;

privileged aspect TributacaoDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TributacaoDataOnDemand: @Component;
    
    private Random TributacaoDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Tributacao> TributacaoDataOnDemand.data;
    
    public Tributacao TributacaoDataOnDemand.getNewTransientTributacao(int index) {
        br.com.walfarma.dominio.Tributacao obj = new br.com.walfarma.dominio.Tributacao();
        obj.setId(new Integer(index).longValue());
        java.lang.String tributacao = "tributacao_" + index;
        if (tributacao.length() > 50) {
            tributacao  = tributacao.substring(0, 50);
        }
        obj.setTributacao(tributacao);
        obj.setCod_tributacao("cod_tributacao_" + index);
        return obj;
    }
    
    public Tributacao TributacaoDataOnDemand.getSpecificTributacao(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Tributacao obj = data.get(index);
        return Tributacao.findTributacao(obj.get_id());
    }
    
    public Tributacao TributacaoDataOnDemand.getRandomTributacao() {
        init();
        Tributacao obj = data.get(rnd.nextInt(data.size()));
        return Tributacao.findTributacao(obj.get_id());
    }
    
    public boolean TributacaoDataOnDemand.modifyTributacao(Tributacao obj) {
        return false;
    }
    
    public void TributacaoDataOnDemand.init() {
        data = br.com.walfarma.dominio.Tributacao.findTributacaoEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Tributacao' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<br.com.walfarma.dominio.Tributacao>();
        for (int i = 0; i < 10; i++) {
            br.com.walfarma.dominio.Tributacao obj = getNewTransientTributacao(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
