// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package br.com.walfarma.dominio;

import br.com.walfarma.dominio.ItemNotaDataOnDemand;
import br.com.walfarma.dominio.Lote;
import br.com.walfarma.dominio.ProdutoDataOnDemand;
import java.util.List;
import java.util.Random;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect LoteDataOnDemand_Roo_DataOnDemand {
    
    declare @type: LoteDataOnDemand: @Component;
    
    private Random LoteDataOnDemand.rnd = new java.security.SecureRandom();
    
    private List<Lote> LoteDataOnDemand.data;
    
    @Autowired
    private ProdutoDataOnDemand LoteDataOnDemand.produtoDataOnDemand;
    
    @Autowired
    private ItemNotaDataOnDemand LoteDataOnDemand.itemNotaDataOnDemand;
    
    public Lote LoteDataOnDemand.getNewTransientLote(int index) {
        br.com.walfarma.dominio.Lote obj = new br.com.walfarma.dominio.Lote();
        obj.setId(new Integer(index).longValue());
        obj.setProduto(produtoDataOnDemand.getRandomProduto());
        obj.setQuantidade(new Integer(index));
        obj.setVencimento(new java.util.GregorianCalendar(java.util.Calendar.getInstance().get(java.util.Calendar.YEAR), java.util.Calendar.getInstance().get(java.util.Calendar.MONTH), java.util.Calendar.getInstance().get(java.util.Calendar.DAY_OF_MONTH), java.util.Calendar.getInstance().get(java.util.Calendar.HOUR_OF_DAY), java.util.Calendar.getInstance().get(java.util.Calendar.MINUTE), java.util.Calendar.getInstance().get(java.util.Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime());
        obj.setFabricacao(new java.util.GregorianCalendar(java.util.Calendar.getInstance().get(java.util.Calendar.YEAR), java.util.Calendar.getInstance().get(java.util.Calendar.MONTH), java.util.Calendar.getInstance().get(java.util.Calendar.DAY_OF_MONTH), java.util.Calendar.getInstance().get(java.util.Calendar.HOUR_OF_DAY), java.util.Calendar.getInstance().get(java.util.Calendar.MINUTE), java.util.Calendar.getInstance().get(java.util.Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime());
        java.lang.String numeroLote = "numeroLo_" + index;
        if (numeroLote.length() > 10) {
            numeroLote  = numeroLote.substring(0, 10);
        }
        obj.setNumeroLote(numeroLote);
        obj.setComissao(java.math.BigDecimal.valueOf(index));
        obj.setItemNota(itemNotaDataOnDemand.getRandomItemNota());
        obj.setQuantidadeCompra(new Integer(index));
        return obj;
    }
    
    public Lote LoteDataOnDemand.getSpecificLote(int index) {
        init();
        if (index < 0) index = 0;
        if (index > (data.size() - 1)) index = data.size() - 1;
        Lote obj = data.get(index);
        return Lote.findLote(obj.get_id());
    }
    
    public Lote LoteDataOnDemand.getRandomLote() {
        init();
        Lote obj = data.get(rnd.nextInt(data.size()));
        return Lote.findLote(obj.get_id());
    }
    
    public boolean LoteDataOnDemand.modifyLote(Lote obj) {
        return false;
    }
    
    public void LoteDataOnDemand.init() {
        data = br.com.walfarma.dominio.Lote.findLoteEntries(0, 10);
        if (data == null) throw new IllegalStateException("Find entries implementation for 'Lote' illegally returned null");
        if (!data.isEmpty()) {
            return;
        }
        
        data = new java.util.ArrayList<br.com.walfarma.dominio.Lote>();
        for (int i = 0; i < 10; i++) {
            br.com.walfarma.dominio.Lote obj = getNewTransientLote(i);
            obj.persist();
            obj.flush();
            data.add(obj);
        }
    }
    
}
